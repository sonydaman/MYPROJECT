extends layout

block content
	form#formAddUser(name="adduser",method="post",enctype="multipart/form-data", action="/uploadFile/newfile")
		input#inputUserName(type="text", placeholder="username", name="username")
		input#inputUserEmail(type="file", name="fileName")
		button#btnSubmit(type="submit") submit